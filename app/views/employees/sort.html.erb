<%= form_tag(sort_employees_path, method: "get") do %>
  <%= select_tag "criterion", options_for_select(option_generator), prompt: "choose"%>
  <%= submit_tag "submit"%>
<% end %>

<% unless @employees.nil? %>
  Order by <%= params["criterion"]%>
  <%= render "employees", :employees => @employees%>
<% end %>